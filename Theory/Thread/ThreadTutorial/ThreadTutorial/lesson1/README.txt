lesson1

1 创建线程对象以后，线程的执行默认开启

2 该如何处理线程的结束？
方法1：调用线程等待目标线程的结束
方法2：将目标线程设置为分离模式，由目标线程自己结束。
Tips:
（1） 如果没有调用 join() 或者 detach()，那么，在析构的时候，该线程实例将会调用 std::terminate()，这会导致整个进程退出。
（2） join() 调用会阻塞调用线程，直到目标线程结束以后，才会返回。